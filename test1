# hi, world
学会了在仓库中新建了一个文件，
在 GitHub 中，不加.md后缀的文件（包括无后缀文件、.txt后缀文件等），默认会被识别为「纯文本文件（Plain Text）」
——GitHub 不会对其进行任何格式渲染，只会原样显示文件内容，也不会支持 Markdown 语法的解析。
如果是其他特定后缀（如.py/.java），GitHub 会根据后缀做语法高亮，但本质还是文本文件；而无后缀文件连语法高亮都没有，就是最基础的纯文本展示。
